#include "completioninfoloading.h"

void LoadModuleIntoCompletionInfo(QString path, Module *mod, CompletionInfo &comp)
{
    /*
    ModuleCompletionInfo &mi = comp.module(path);
    mi.name = mod->name();

    for(int i=0; i<mod->declCount(); i++)
    {
        shared_ptr<ProceduralDecl> pd = mod->decl();
    }
    for(int i=0; i<mod->usedModuleCount(); i++)
    {
        ModuleCompletionInfo &mi = comp.modules();
    }
    */
}
